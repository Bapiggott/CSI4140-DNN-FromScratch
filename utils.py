import torch
import matplotlib.pyplot as plt

def accuracy(predictions, labels):
    """
    predictions shape: (batch_size, num_classes)
    labels shape: (batch_size)
    Returns: float representing % correct
    """
    pass

def plot_metrics(train_accs, test_accs, train_losses, test_losses):
    """
    Utility to plot training/test accuracy and loss curves.
    """
    pass
